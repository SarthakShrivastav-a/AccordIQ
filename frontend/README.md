# AccordIQ Frontend

Next.js admin panel for AccordIQ.

```powershell
cd frontend
npm install
npm run dev
npm run lint
npm run test
npm run build
```

Set `NEXT_PUBLIC_ACCORDIQ_API_BASE_URL` to point at the FastAPI backend.
